<?xml version="1.0" encoding="utf-8"?>
<adaptive-icon xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Full Tuned-audio artwork (black field, white screen, waveform). Foreground is
         transparent so the whole image shows through, masked to the launcher shape. -->
    <background android:drawable="@drawable/ic_launcher_tuned" />
    <foreground android:drawable="@android:color/transparent" />
</adaptive-icon>
