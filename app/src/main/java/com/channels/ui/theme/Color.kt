package com.channels.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Monochrome palette matching the Light Phone III: white ink on a pure-black
 * ground, with a few grays for structure. High contrast, no accent hue.
 * (Names describe role: Ink = foreground/text, Paper = background.)
 */
val Ink = Color(0xFFF2F2F2)        // primary text / active (near-white)
val Paper = Color(0xFF000000)      // background (black)
val Graphite = Color(0xFFCFCFCF)   // strong secondary
val Slate = Color(0xFF9A9A9A)      // muted text / inactive
val Ash = Color(0xFF6E6E6E)        // hints / placeholders
val Hairline = Color(0xFF262626)   // dividers / borders on black
val Mist = Color(0xFF141414)       // subtle raised surface
